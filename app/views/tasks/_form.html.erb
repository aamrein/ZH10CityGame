<%= form_with(model: task, local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :task_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :task_description %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :task_location %>
  </div>

  <div class="field">
    <%= form.label :duration_min %>
    <%= form.number_field :duration_min, id: :task_duration_min %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :task_cost %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment, id: :task_comment %>
  </div>

  <div class="field">
    <%= form.label :optional %>
    <%= form.check_box :optional, id: :task_optional %>
    <p>Der Betrag wird beim Spielende automatisch verbucht, wenn eine Aufgabe nicht als Optional markiert ist. Dies ist nur bei negativen Beträgen sinnvoll.</p>
  </div>

  <div class="field">
    <%= form.label :settlement_immediately %>
    <%= form.check_box :settlement_immediately, id: :task_settlement_immediately %>
    <p>Beim erledigen wird der Betrag unmittelbar verbucht.</p>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
